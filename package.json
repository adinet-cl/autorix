{
  "name": "@adinet/autorix-root",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root for Autorix packages",
  "packageManager": "pnpm@10.10.0",
  "workspaces": [
    "packages/autorix",
    "packages/autorix-nestjs",
    "packages/autorix-typeorm-adapter",
    "packages/autorix-prisma-adapter"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "class-validator": "^0.14.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "eslint": "^9.26.0",
    "prettier": "^3.5.3",
    "tsup": "^8.4.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  }
}
